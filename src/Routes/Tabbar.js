import React from 'react';
import {
    StyleSheet,
    Text,
    View,
    ScrollView,
    Image,
    TouchableOpacity,
    FlatList,
    ImageBackground,
    I18nManager,
    SafeAreaView,
    Alert,
    Animated,
    Dimensions,
    Easing
} from 'react-native';
const Tabbar = (props) => {
    console.log('load')
    return (
        <View>

        </View>
    );
};

export default Tabbar;
